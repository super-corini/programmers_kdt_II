DAY 1 과제를 진행하면서 겪었던 수 많은 실패
 >> 점프 투 플라스크를 참고하여 ORM을 사용해 보려 했지만 models.py를 생성 후 app.py에서 import models로 테이블 생성을 할 수 없었던 문제
   ㄴ 해당 문제는 구글링 결과 파일의 위치와 관련이 있는것으로 파악하였으나 10시간 이상 문제가 해결되지 않아 포기
 >> 익숙한 mysql을 이용하기 위해 mysql 8.0 버전을 설치 오랜만에 사용해서 인지 mysql server를 만들려는 엉뚱한 행동을 하게됨 3시간을 날리고 mysql은 server가 필요하지 않다는 것을
    알게됨
   ㄴ study 스키마 생성 후 menus 테이블을 만드는 2가지 과정만이 필요하다는것을 알게 됨
 >> 데이터 베이스를 연결하지 않은 경우 app.py의 실행 결과값이 초기화 되지 않아 다른 method의 실행 결과가 정상적으로 반영되지 않는 현상 발생
   ㄴ 오랜시간 문제를 해결하지 못했지만 브라우저 자체에 쿠기 값이 문제를 일으키는 것은 아닌가 싶어 쿠기 삭제 했으나 해결되지 않았고 컴퓨터를 재부팅 하자 해결됨
 >> ORM의 장점은 SQL을 잘 모르는 경우 익숙한 Python 언어로 Database를 이용할 수 있음
 >> 코딩 테스트를 위한 알고리즘 공부만 하다 프로그램을 만들기 위해 Pyshon을 사용해 보니 또 다른 재미를 느꼈고 더 많은 것을 알게 됨
 >> githun에서 제대로 clone하지 않으면 Fullrequest사 바교할 대상이 없다는 오류를 만날 수 있음
    ㄴ 제대로된 clone 후 모든 파일을 add commit push하고 나서야 정상적으로 Fullrequest할 수 있었음 (시간 많이 잡아 먹음........)


Bonus2에 사용된 환경
- mysql 8.0
- pycharm
  ㄴPython 3.8

Bonus2 문제에 사용된 menus 테이블
 ㄴAUTO_INCREMENT : 값 자동 증가 옵션
 ㄴPRIMARY KEY : index에 성격이 강해 기본키로 설정
 
create table menus(
   	id int NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name varchar(256) not null,
    price int not null
);

